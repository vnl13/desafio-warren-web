import styled from 'styled-components';

export const Container = styled.header`
  background: var(--pink);
  a {
    color: var(--white);
  }
  height: 100px;
  width: 100%;
  padding: 0 5%;
  display: flex;
  align-items: center;
  font-size: 3rem;
  font-weight: 700;
`;
